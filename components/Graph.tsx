import {Flex, Box, Text, Select, Spacer } from "@chakra-ui/react";

const Graph = () => {
    return(
        <Text fontSize="5xl" color="whiteAlpha.800" >
            Graph
        </Text>
    );
};

export default Graph;